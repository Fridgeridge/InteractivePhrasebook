<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="se.chalmers.phrasebook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        android:name="android.support.multidex.MultiDexApplication">
        <activity
            android:name=".gui.HelperActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".gui.FirstUsageActivity"/>
        <activity android:name=".gui.HomeScreenActivity" />
        <activity android:name=".gui.MainActivity" />
        <activity android:name=".gui.PhraseListActivity" />
        <activity android:name="gui.TranslatorActivity" />
    </application>

</manifest>
