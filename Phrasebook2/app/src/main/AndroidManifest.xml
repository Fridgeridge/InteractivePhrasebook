<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="se.chalmers.phrasebook">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <!--
        <activity android:name=".gui.activities.HelperActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> -->

        <activity android:name=".gui.activities.HelperActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".gui.activities.NavigationActivity">

        </activity>

        <activity
            android:name=".gui.activities.HomeScreenActivity"
            android:label="@string/phrasebook_activity" />
        <activity
            android:name=".gui.activities.PhraseListActivity"
            android:label="@string/Phrases_activity" />
        <activity
            android:name=".gui.activities.TranslatorActivity"
            android:label="@string/translator_activity" />
        <activity
            android:name=".gui.activities.LanguageActivity"
            android:label="Language" />
        <activity
            android:name=".gui.activities.AddToPhrasebookActivity"
            android:label="Add Phrase" />
        <activity
            android:name=".gui.activities.FirstUsageActivity"
            android:label="@string/title_activity_main" />
    </application>

</manifest>
